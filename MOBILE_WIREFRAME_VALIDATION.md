# Mobile Wireframe Validation

The Mobile preview now renders a dedicated campaign composition instead of compressing the desktop grid. The header hides desktop navigation links, the Hero stacks its copy and visual, Multi column renders one item per row, Image + content stacks its visual and copy, and Text, FAQ, and Footer preserve a readable single-column hierarchy.

The regenerated Mobile JPG reflects the same responsive structure. It contains the simplified header, stacked Hero, one-column collection items, full-width secondary See All action, stacked supporting image and content, FAQ rows, and a mobile footer without editor chrome.

The browser workflow explicitly verifies every campaign template in mobile mode: Header navigation is hidden, Hero copy and visual stack, Multi column uses one item per row, Image + content preserves image-first stacking, Text block stacks its two-column narrative, FAQ keeps its heading above accordion rows, and Footer keeps its company statement above its navigation groups. The Mobile JPG is regenerated from the same mobile renderers.
